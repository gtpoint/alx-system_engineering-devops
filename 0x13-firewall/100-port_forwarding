# Copy the existing ufw configuration file for backup
cp /etc/ufw/before.rules /etc/ufw/before.rules.backup

# Open the ufw configuration file in a text editor
nano /etc/ufw/before.rules

# Add the following lines to the ufw configuration file
*nat
:PREROUTING ACCEPT [0:0]
-A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80
COMMIT

# Save the file and exit the text editor (for nano, press Ctrl+O, Enter, Ctrl+X)

# Restart the ufw service to apply the changes
ufw disable
ufw enable
